/**
 * MenuController
 *
 * @description :: Server-side actions for handling incoming requests.
 * @help        :: See https://sailsjs.com/docs/concepts/actions
 */

module.exports = {
  

  /**
   * `MenuController.imagePath()`
   */
  imagePath: async function (req, res) {
    return res.json({
      todo: 'imagePath() is not implemented yet!'
    });
  },

  /**
   * `MenuController.name()`
   */
  name: async function (req, res) {
    return res.json({
      todo: 'name() is not implemented yet!'
    });
  },

  /**
   * `MenuController.description()`
   */
  description: async function (req, res) {
    return res.json({
      todo: 'description() is not implemented yet!'
    });
  },

  /**
   * `MenuController.price()`
   */
  price: async function (req, res) {
    return res.json({
      todo: 'price() is not implemented yet!'
    });
  },

  /**
   * `MenuController.restaurantId()`
   */
  restaurantId: async function (req, res) {
    return res.json({
      todo: 'restaurantId() is not implemented yet!'
    });
  }

};

